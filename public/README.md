# Dossier Public

Placez votre image de profil ici et nommez-la `placeholder-image.jpg` (ou modifiez le chemin dans `src/components/Hero.vue`).

L'image sera accessible via `/placeholder-image.jpg` dans votre application.

**Recommandations pour l'image :**
- Format : JPG, PNG ou WebP
- Taille recommandée : 800x1000px ou ratio similaire
- Poids : < 500KB pour un chargement optimal

